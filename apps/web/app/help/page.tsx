import Link from "next/link";
import { Footer, PublicHeader } from "@/components/PublicChrome";
import { SupportChat } from "@/components/SupportChat";
import { relayOpsService } from "@/lib/service";
export const metadata = { title: "Help centre" };
export default async function HelpPage() { const articles = await relayOpsService.listArticles(); return <><PublicHeader /><main><section className="help-hero"><div className="container"><span className="eyebrow" style={{ color: "#8ed8c2" }}>RelayOps help centre</span><h1>What can we help you solve?</h1><p>Practical guidance for running your fictional RelayOps workspace.</p><div className="search"><label className="sr-only" htmlFor="help-search">Search help articles</label><input id="help-search" placeholder="Search articles (visual demo)" /></div></div></section><section className="section section-soft"><div className="container"><div className="section-heading"><span className="eyebrow">Popular guides</span><h2>Start with the essentials</h2><p>Original, concise demo content—not a production support corpus.</p></div><div className="article-grid">{articles.map((article) => <Link className="article-card" href={`/help/${article.slug}`} key={article.slug}><span className="eyebrow" style={{ fontSize: ".66rem" }}>{article.category}</span><h3>{article.title}</h3><p>{article.summary}</p><div className="article-meta">{article.readTime} read · Updated {article.updated}</div></Link>)}</div></div></section></main><Footer /><SupportChat /></>; }
